################### sqsbeat Configuration Example #########################

############################# sqsbeat ######################################

sqsbeat:
  # Defines how often an event is sent to the output
  period: 1s
  credentials.accessKey: "accesskey"
  credentials.secretKey: "secretKey"
